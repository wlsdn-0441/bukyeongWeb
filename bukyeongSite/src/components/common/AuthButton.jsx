/**
 * Authentication Button Component
 *
 * Google 로그인 기능 제거됨 - 익명 로그인만 사용
 */

import './AuthButton.css';

export default function AuthButton({ user }) {
  // Google 로그인 기능이 제거되어 익명 사용자만 존재
  // 버튼을 표시하지 않음
  return null;
}
